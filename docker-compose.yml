version: '3.9'
services:
  web:
    build: .
    command: sh -c "python manage.py migrate && python manage.py runserver localhost:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
# No database service needed for SQLite3
